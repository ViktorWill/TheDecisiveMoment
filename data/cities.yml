# Cities spotforge builds bundles for — docs/SPOTFORGE.md §1.
#
# Districts exist so Overpass queries stay under the server timeout on large
# cities, and so the UI can say "12 new spots in Brooklyn" rather than treating
# a city as one undifferentiated blob.
#
# ---------------------------------------------------------------------------
# All five boroughs — restored after PR #16.
#
# This was temporarily scoped to Manhattan alone: the first real run showed
# that SpotMerger.merge was O(n²) with no spatial index, and that the shared
# RequestRunner serialised Commons behind Overpass's one-at-a-time policy.
# Both are fixed now — merge buckets candidates into a grid instead of
# comparing every candidate against every other, and Commons gets its own,
# more permissive concurrency — so the full city fits inside the monthly
# workflow's time budget again.
# ---------------------------------------------------------------------------

- id: us-nyc
  name: New York City
  country: US
  center: { lat: 40.7736, lon: -73.9712 }
  bbox: { minLat: 40.4774, minLon: -74.2591, maxLat: 40.9176, maxLon: -73.7004 }
  districts:
    - { name: Manhattan,     bbox: { minLat: 40.6980, minLon: -74.0200, maxLat: 40.8800, maxLon: -73.9070 } }
    - { name: Brooklyn,      bbox: { minLat: 40.5700, minLon: -74.0420, maxLat: 40.7395, maxLon: -73.8330 } }
    - { name: Queens,        bbox: { minLat: 40.5400, minLon: -73.9630, maxLat: 40.8010, maxLon: -73.7004 } }
    - { name: The Bronx,     bbox: { minLat: 40.7855, minLon: -73.9340, maxLat: 40.9176, maxLon: -73.7654 } }
    - { name: Staten Island, bbox: { minLat: 40.4774, minLon: -74.2591, maxLat: 40.6514, maxLon: -74.0345 } }

# Munich core: Altstadt out to roughly the Mittlerer Ring, no districts —
# a walkable single city, not five boroughs, and comfortably inside one
# Overpass query even before #19's tightened clauses. The box is a
# best-effort estimate from a map, not measured: it aims to clear Hauptbahnhof
# in the west to Haidhausen in the east, and Sendlinger Tor in the south to
# Münchner Freiheit / the start of the Englischer Garten in the north. Worth
# a glance on an actual map before the first build — widening it costs
# nothing but a slower query, missing a neighbourhood costs a walk with no
# spots in it.
- id: de-munich
  name: Munich
  country: DE
  center: { lat: 48.1374, lon: 11.5755 }
  bbox: { minLat: 48.1000, minLon: 11.4900, maxLat: 48.1850, maxLon: 11.6300 }
